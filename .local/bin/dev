#!/bin/sh
notefile=$(find ~/Dropbox/NOTES/ -maxdepth 1 -type f -iname "*$1*" | head -n 1)
if [ $1 == "mind" ]; then
	notefile="/home/flex/Dropbox/NOTES/MIND.md"
	st -t Server -e zsh -c "cd ~/.local/src/mind; nvm use v14.21.3; npm run dev" &
elif [ $1 == "sandhamn" ]; then
	st -t Server -e zsh -c "cd ~/.local/src/sandhamn; nvm use v16.18.1; npm run dev" &
else
	st -t Server -e zsh -c "cd ~/.local/src/$1; npm run dev" &
fi
st -t Timer -e zsh -c "termdown --no-figlet" &
sleep 0.2 && st -t Git -e zsh -c "cd ~/.local/src/$1; zsh" &
st -t Notes -e zsh -c "cd ~/Dropbox/NOTES/; nvim \"$notefile\"" &
sleep 0.5 && brave --new-window http://localhost:3000 &
sleep 2 && st -e zsh -c "cd ~/.local/src/$1; nvim -c \"e .\"" &
