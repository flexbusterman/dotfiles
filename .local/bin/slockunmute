#!/usr/bin/env bash
/home/flex/.local/bin/mute
[[ -f /usr/bin/slock ]] && /usr/bin/slock || /usr/local/bin/slock
/home/flex/.local/bin/remaps
/home/flex/.local/bin/unmute
sleep 6
[[ $(pactl list sinks | grep -i "bluetooth") ]] && /home/flex/.local/bin/unmute
