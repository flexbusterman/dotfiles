#!/bin/sh

# This script is the SUDO_ASKPASS variable, meaning that it will be used as a password prompt if needed.
# The -P flag hides typed characters and requires the password dmenu patch

# dmenu -fn Monospace-18 -P -p "$1" <&- && echo
# dmenu -p "$1" <&- && echo
dmenu -P -p "$1" <&-
