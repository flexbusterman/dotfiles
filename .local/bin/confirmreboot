#!/bin/bash
answer=$(echo -e "yes\nno" | wmenu -f "BigBlueTerm437 Nerd Font Mono 9" -n "#FFFFFF" -p "Really reboot? ")
if [[ "$answer" == "yes" ]]; then
	reboot
else
	echo "Reboot cancelled"
	notify-send "Reboot cancelled"
fi
