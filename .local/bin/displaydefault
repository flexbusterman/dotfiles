#!/bin/sh
secondary=$(xrandr -q | grep -w connected | awk '/^[^eDP1]/ {print $1}')
# echo $secondary
xrandr --output eDP1 --auto --scale 1.0x1.0 --output $secondary --above eDP1 --auto --scale 1.0x1.0

setbg
remaps
killall dunst
setsid -f dunst
