#!/bin/bash

STATUS=$(mullvad status | awk '{print $NF}')
if [[ $STATUS == "Connected" ]]; then
    echo "Mullvad connected, not checking mail."
    exit 1
else
    echo "Checking mail"
    mailsync
fi
